//
//  AppColors.swift
//  Nova
//
//  Created by Aarush Agarwal on 7/11/25.
//

import SwiftUI

struct AppColors {
    // static let background = Color(hex: "#323232")
    static let background = Color(hex: "#1C1C1C")
    static let userMessageBackground = Color(hex: "#4D4D4D")
    // static let textFieldBackground = Color(hex: "#4D4D4D")
    static let textFieldBackground = Color(hex: "#272727")
    static let textFieldBorder = Color(hex: "#5A5A5A")
    static let primaryText = Color.white
    static let secondaryText = Color.gray
    static let accentBlue = Color.blue
    static let accentBlueMuted = Color.blue.opacity(0.6)
    
    // Markdown-specific colors
    static let codeBlockBackground = Color(hex: "#2D2D2D")
    static let codeBlockBorder = Color(hex: "#404040")
    static let inlineCodeBackground = Color(hex: "#363636")
    static let blockQuoteAccent = Color(hex: "#4A90E2")
    static let blockQuoteBackground = Color(hex: "#252525")
}
